# React + TypeScript + Vite

Use `npm run dev` or `npx vite` after you've installed dependencies using `npm i`.
To download unity app so it can be served in the website run, `npm run fetch-unity-assets`
